# Domain Documentation

The monorepo context map is [`CONTEXT-MAP.md`](../../CONTEXT-MAP.md). It is the single entry point for package ownership and domain terminology.

Cross-package architectural decisions belong in `docs/adr/`. Package-local implementation details stay beside the owning package and should not be duplicated in the context map.
