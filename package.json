{
  "version": "0.0.1",
  "packageManager": "pnpm@10.34.5",
  "scripts": {
    "capture:site-screenshot": "node scripts/capture-site-screenshot.mjs",
    "check:docs:content": "vitest run --config vitest.docs.config.ts && node check-docs.mjs",
    "check:docs": "pnpm check:docs:content && pnpm --dir packages/docs-site check",
    "check:versioning-build": "pnpm --dir packages/docs-site exec sveltepress versions build --locale zh --draft-only && pnpm --dir packages/docs-site exec sveltepress versions build --locale bn --draft-only && pnpm --filter @sveltepress/docs-site build && node scripts/check-version-management-build.mjs",
    "lint": "eslint",
    "format": "prettier \"./**/*.svelte\" --check",
    "release": "changeset publish",
    "test:scripts": "vitest run --config vitest.scripts.config.ts",
    "test:vite": "cd packages/vite && npx vitest run && cd ../..",
    "test:cli": "cd packages/cli && npx vitest run && cd ../..",
    "test:theme-default": "cd packages/theme-default && npx vitest run && cd ../..",
    "test:theme-blog": "cd packages/theme-blog && npx vitest run && cd ../..",
    "test": "pnpm test:scripts && pnpm test:cli && pnpm test:vite && pnpm test:theme-default && pnpm test:theme-blog",
    "prepare": "husky && git config core.hooksPath .husky"
  },
  "dependencies": {
    "@changesets/changelog-github": "catalog:"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:",
    "@changesets/cli": "catalog:",
    "changelogithub": "catalog:",
    "eslint": "catalog:",
    "eslint-plugin-svelte": "catalog:",
    "happy-dom": "catalog:",
    "husky": "catalog:",
    "lint-staged": "catalog:",
    "playwright": "catalog:",
    "prettier": "catalog:",
    "prettier-plugin-svelte": "catalog:",
    "svelte": "catalog:",
    "taze": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "zx": "catalog:"
  }
}
