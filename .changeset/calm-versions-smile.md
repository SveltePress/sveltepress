---
"@sveltepress/vite": patch
"@sveltepress/theme-default": patch
---

Keep the version runtime browser-safe, and render lifecycle states as compact badges without wrapping date-based version labels.
