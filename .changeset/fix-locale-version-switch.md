---
'@sveltepress/vite': patch
'@sveltepress/theme-default': patch
'@sveltepress/cli': patch
---

Keep the documentation version and heading when switching languages, and include every locale's version history in a default `versions build`
