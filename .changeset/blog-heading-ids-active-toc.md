---
'@sveltepress/theme-blog': patch
---

Generate slugified `id` attributes on headings via a new rehype plugin, and rewrite the table of contents to highlight the section currently in view (Nuxt-style scroll tracking with rAF throttle).
