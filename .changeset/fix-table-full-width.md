---
'@sveltepress/theme-default': patch
---

Fix markdown table layout so tables expand to full width (`width: 100%; display: table;`) and do not collapse horizontally to content width with empty trailing space.
