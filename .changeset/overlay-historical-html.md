---
'@sveltepress/vite': minor
'@sveltepress/cli': minor
---

Run Vite on current locale trees only during `versions build`, then overlay frozen `/v/` HTML that shares the current `_app` assets. Current-site Pagefind skips historical version prefixes.
